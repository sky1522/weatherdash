# PROGRESS

최종 갱신: 2026-09-06

## 현재 단계

**P0 — 스캐폴딩 및 응답 실측** (완료). 다음은 P1 파서·도메인 모델.

레퍼런스 캡처 확보: 2026년 제24호 태풍 크로반(KROVANH), 2026-09-06 10:00 발표.
09시 현재 25.9°N 129.3°E, 992 hPa, 최대풍속 21 m/s, 강풍반경 250 km(남서 약 120 km 예외),
폭풍반경 `-`, 강도 1, 북동진 14 km/h. 파서·렌더 검증의 기준 케이스로 사용한다.

## 단계별 상태

| 단계 | 상태 | 비고 |
|---|---|---|
| P0 스캐폴딩·실측 | ✅ 완료 | 픽스처 20건 커밋. 🔶 6건 중 6건 해소 |
| P1 파서·도메인 | ⬜ 미착수 | 착수 가능 |
| P2 경로 렌더 | ⬜ 미착수 | |
| P3 반경·콘 | ⬜ 미착수 | 예외섹터 해석 리스크 |
| P4 시간축·패널 | ⬜ 미착수 | |
| P5 운영 견고화 | ⬜ 미착수 | |

## 결정 기록

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-06 | `disp=1, help=2`로 고정 | 구분자 응답이 파싱 안정적. `disp=0` 고정폭은 컬럼 폭 명세가 없어 배제 |
| 2026-09-06 | Worker 프록시 필수 | `authKey`가 쿼리스트링에 노출되는 구조라 클라이언트 직접 호출 불가 |
| 2026-09-06 | `typ01` 범위로 한정 | 진행 중 태풍만 제공. 과거 태풍은 별도 트랙 |
| 2026-09-06 | Worker에서 EUC-KR→UTF-8 디코딩 | 응답이 EUC-KR 확정. 프론트에 UTF-8 JSON만 넘긴다 |
| 2026-09-06 | 결측 판정을 `-999`(수치) / `-`(방위) 두 토큰으로 고정 | 픽스처 48개 데이터행 전수 확인. 빈칸·`-9`는 나타나지 않음 |
| 2026-09-06 | 부재 판정을 "HTTP 200 + 데이터행 0개"로 정의 | 부재 시에도 200이 온다. HTTP 상태만으로는 구분 불가 |
| 2026-09-06 | 필드는 이름이 아니라 **위치**로 파싱 | 응답 내 `ER25R`/`ER25` 표기가 불일치. 구분자 응답에 필드명은 없다 |

## 해소된 질문 ✅

근거는 전부 `fixtures/raw/` 의 실제 응답 바이트다. 수집 대상은 2026년 제24호 태풍
크로반(KROVANH), `YY=2026 typ=24 seq=21 mode=1`.

### 1. 응답 인코딩 — **EUC-KR** ✅

응답 헤더가 `Content-Type: text/plain;charset=EUC-KR` 이고, 실제 바이트도 일치한다.
`fixtures/raw/typ_data_disp1_help2.txt` 를 UTF-8로 디코딩하면 83번째 바이트에서 실패하고,
EUC-KR로 디코딩하면 전체가 성공한다. 목록·상세·시점조회 세 엔드포인트 모두 동일하다.

```
b'... ,SW,140,ÀÏº» ¿ÀÅ°³ª¿Í ...'
   EUC-KR 디코딩 → "일본 오키나와"
```

Worker는 업스트림 바이트를 EUC-KR로 디코딩한 뒤 UTF-8 JSON으로 내보낸다.
🚨 이름만 깨지고 숫자는 멀쩡하므로, 디코딩 회귀는 픽스처 테스트로만 잡힌다.

### 2. 결측 표기 — **수치는 `-999`, 방위는 `-`** ✅

`typ_data` / `typ_now` 의 데이터행 48개를 전수 조사한 결과다.
빈 문자열도 `-9`도 나타나지 않는다.

| 필드 | 관측된 고유값 |
|---|---|
| `RAD25` (폭풍반경) | `-999` (48/48 전부 결측) |
| `ER25R` (폭풍 예외반경) | `-999` (48/48) |
| `ED25` (폭풍 예외방향) | `-` (48/48) |
| `RAD15` (강풍반경) | `240`, `250`, `260`, `-999` |
| `ER15` (강풍 예외반경) | `110`~`160`, `-999` |
| `ED15` (강풍 예외방향) | `SW`, `NNW`, `NW`, `-` |

🚨 **강풍반경도 결측된다.** 폭풍반경만 결측이라고 가정하면 안 된다. `+36h` 예측에서
최대풍속이 15 m/s로 약해지자 `RAD15=-999`, `ED15=-`, `ER15=-999` 가 동시에 나타났다.

```
1,2026,24,21,36,202609060000,202609071200,28.5,132.8,NNE,16,996,15,-999,-999,110,-,-999,...
```

🚨 `-999` 를 숫자로 파싱하면 **반경 −999 km** 가 되어 렌더 단계에서 조용히 깨진다.
파서는 `-999` 와 `-` 를 `null` 로 정규화한다.

### 3. 진행 중 태풍 부재 시 응답 — **HTTP 200 + 데이터행 0개** ✅

오류가 아니다. 상태코드만으로는 "없음"과 "실패"를 구분할 수 없다. 🚨

| 조회 | HTTP | 본문 |
|---|---|---|
| `typ_data` `typ=99` (없는 번호) | 200 | 0 바이트, 완전히 빈 응답 |
| `typ_now` `tm=190001010000` | 200 | `#7777END
` (9바이트) |

부재 형태가 엔드포인트마다 다르다는 점이 함정이다. 특히 `typ_data` 의 0바이트 응답은
**응답 잘림과 구분되지 않는다.** 판정 규칙을 아래로 고정한다.

- HTTP 200 + 데이터행 0개 → `status: 'none'`
- HTTP 비200, 네트워크 실패, JSON 오류 본문 → `status: 'error'`

인증 실패는 별도 경로로 JSON이 온다. 이건 UTF-8이다.

```
HTTP 401  {"result":{"status":401,"message":"유효한 인증키가 아닙니다."}}
```

### 4. `ER25R` 필드명 — **명세 오타가 아니다. 다만 응답 내부에서 불일치한다** ✅

`help=1` 응답 자체가 두 표기를 함께 쓴다. 항목 설명은 `ER25R`, 컬럼 헤더는 `ER25` 다.

```
# 21. ER25R  : 풍속 25km 영역에서 예외반경 (km)
# FT   YY  TYP ... ED15  ER15 LOC----------------ED25 ER25
```

`disp=1` 구분자 응답에는 필드명이 실려 오지 않으므로 파싱에 영향은 없다.
🚨 파서는 필드명이 아니라 **위치(21번째 필드)** 로 참조한다. 이름 기준으로 매핑하면
표기 불일치에 걸린다.

### 5. `RAD15`/`RAD25` 대응 — **강풍/폭풍반경이 맞다. 단위 오기는 다른 줄에 있다** ✅

`help=1` 항목 설명은 `RAD15`/`RAD25` 를 정확히 적고 있다.

```
# 14. RAD15  : 15m/s 반경 (km)
# 15. RAD25  : 25m/s 반경 (km)
# 16. RAD    : 70%이상 예상확률반경 (km)
```

"15km" 오기는 `RAD` 계열이 아니라 예외방향·예외반경 설명에 있다.
`# 17. ED15 : 풍속 15km 영역에서 예외방향` — 문맥상 15 m/s가 맞다.

레퍼런스 캡처와 완전히 일치한다. 아래는 `seq=21` 분석행이다.

```
0,2026,24,21,0,202609060000,202609060000,25.9,129.3,NE,14,992,21,250,-999,0,SW,120,...,-,-999,=
```

| 화면 표기 | 필드 | 값 |
|---|---|---|
| 25.9°N 129.3°E | `LAT`/`LON` | 25.9 / 129.3 |
| 992 hPa | `PS` | 992 |
| 최대풍속 21 m/s | `WS` | 21 |
| 강풍반경 250 km[남서 약 120 km] | `RAD15`/`ED15`/`ER15` | 250 / SW / 120 |
| 폭풍반경 `-` | `RAD25` | -999 (결측) |
| 북동진 14 km/h | `DIR`/`SP` | NE / 14 |

`RAD`(70% 확률반경)는 분석행에서 전부 `0` 이고 예측행에서만 `40`/`80`/`110` 이다.
🚨 SKILL.md 2부의 "현재 위치 원 반경 0" 분기가 실제로 필요하다는 뜻이다.

### 6. 발표 시각 4·10·16·22시 — **KST** ✅

분석시각(`TYP_TM`)은 UTC `00/06/12/18` 정시로만 나타난다. KST로는 `09/15/21/03` 이다.

```
202609010000, 202609010600, 202609011200, 202609011800, 202609020000, ...
```

레퍼런스 캡처는 "2026-09-06 10:00 발표, 09시 현재"이고, 그 09시 값이 위 분석행
`TYP_TM=202609060000`(UTC) 과 정확히 일치한다. 즉 09 KST = 00 UTC 분석 → 10 KST 발표다.
같은 관계를 네 회차에 적용하면 `03/09/15/21 KST` 분석 → `04/10/16/22 KST` 발표가 된다.

발표 주기는 KST 기준으로 잡고, 신선도 배지는 `TYP_TM`(UTC)에 1시간을 더한 값을
발표시각 근사로 쓴다. 🚨 시각 변환은 `lib/time.ts` 한 곳에서만 한다.

## 미해결 질문 🔶

### 3. 예외방향·예외반경의 섹터 의미 🔶

픽스처로 해소되지 않았다. 응답은 `ED15=SW, ER15=120` 처럼 방위 하나와 반경 하나만 준다.
그 방위 **한 점만** 120 km인지, 남서 사분면 전체가 120 km인지 판별할 정보가 응답에 없다.

SKILL.md 2부의 해석 (A)/(B) 파라미터화를 유지한다. 기본값 (A). 코드에 하나를 박지 않는다.
해소하려면 기상청 화면의 반경 폴리곤을 좌표로 대조하거나 예보 해설을 확인해야 한다.

### 8. `typ_now.php` 의 `tm` 기준 🔶

"과거 12시간 내 발표" 가 발표시각 기준인지 분석시각 기준인지 미확정이다.
`tm=202609060300`(UTC) 조회 결과가 `typ_data seq=21` 과 사실상 동일했는데,
해당 발표의 분석시각(00 UTC)과 발표시각(약 01 UTC)이 12시간 창 안에 모두 들어가
두 해석을 구분하지 못한다.

해소하려면 경계에 걸치는 `tm` 값으로 조회해 응답이 갈리는 지점을 찾아야 한다.
P4 폴링 스케줄 설계 전까지 확정하면 된다.

## 확인된 함정 🚨

- 인자 생략(`YY`, `typ`, `seq`) 시 응답이 시점에 따라 달라짐 → 운영 코드에서 전면 명시
- `FT` 무시하고 시각순 정렬 시 분석·예측 혼입 → 화면상 티가 안 남
- 서로 다른 `seq`의 예측을 합치면 경로 꼬임
- 결측 반경을 0으로 채우면 반경 0 원이 렌더됨
- 날짜변경선 통과 시 경로가 지도를 가로지름
- 결측이 `-999`라 숫자로 파싱하면 반경 −999 km가 되어 그대로 렌더됨 (실측 확인)
- 강풍반경(`RAD15`)도 결측된다. 폭풍반경만 결측이라 가정하면 안 됨 (실측 확인)
- 부재 응답이 HTTP 200이고, `typ_data`는 0바이트라 응답 잘림과 구분되지 않음 (실측 확인)
- `git core.autocrlf=true` 환경에서 픽스처가 커밋 시점에 줄바꿈 변환으로 깨짐
  → `.gitattributes`로 차단하고 sha256 검사 테스트로 감시

## 다음 액션

P1 착수. 순서는 아래.

1. `lib/time.ts` — UTC→KST 변환 단일 지점, 16방위 변환표
2. `parseTyphoonList()` / `parseTyphoonData()` — `disp=1, help=2` 기준,
   필드는 **위치**로 참조. `-999`/`-` → `null`
3. `FT` 기준 `analysis[]` / `forecast[]` 분리, `(YY, TYP, SEQ)` 그룹핑
4. Worker에 EUC-KR→UTF-8 디코딩과 정규화 JSON 응답 추가,
   `status: 'ok' | 'none' | 'error'` 구분
5. 픽스처 20건 전부에 대한 파서 스냅샷 테스트
