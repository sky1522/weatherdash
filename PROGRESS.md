# PROGRESS

최종 갱신: 2026-09-06

## 현재 단계

**P0 — 스캐폴딩 및 응답 실측** (착수 전)

레퍼런스 캡처 확보: 2026년 제24호 태풍 크로반(KROVANH), 2026-09-06 10:00 발표.
09시 현재 25.9°N 129.3°E, 992 hPa, 최대풍속 21 m/s, 강풍반경 250 km(남서 약 120 km 예외),
폭풍반경 `-`, 강도 1, 북동진 14 km/h. 파서·렌더 검증의 기준 케이스로 사용한다.

## 단계별 상태

| 단계 | 상태 | 비고 |
|---|---|---|
| P0 스캐폴딩·실측 | ⬜ 미착수 | 픽스처 수집이 최우선 |
| P1 파서·도메인 | ⬜ 미착수 | P0 🔶 해소 후 시작 |
| P2 경로 렌더 | ⬜ 미착수 | |
| P3 반경·콘 | ⬜ 미착수 | 예외섹터 해석 리스크 |
| P4 시간축·패널 | ⬜ 미착수 | |
| P5 운영 견고화 | ⬜ 미착수 | |

## 결정 기록

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-06 | `disp=1, help=2`로 고정 | 구분자 응답이 파싱 안정적. `disp=0` 고정폭은 컬럼 폭 명세가 없어 배제 |
| 2026-09-06 | Worker 프록시 필수 | `authKey`가 쿼리스트링에 노출되는 구조라 클라이언트 직접 호출 불가 |
| 2026-09-06 | `typ01` 범위로 한정 | 진행 중 태풍만 제공. 과거 태풍은 별도 트랙 |

## 미해결 질문 🔶

작업 중 확인되면 즉시 여기서 ✅로 옮기고 근거(응답 원문 일부)를 남긴다.

1. **응답 인코딩** — 태풍 한글명(`TYP_NAME`) 반환 시 UTF-8인지 EUC-KR인지. Worker에서
   디코딩을 잘못하면 이름만 깨지고 나머지는 정상이라 발견이 늦다.
2. **결측 표기** — 폭풍반경이 없는 경우(레퍼런스의 `-`) API가 무엇을 반환하는가.
   빈 문자열 / `-9` / `-999` / `-` 중 무엇인지에 따라 파서 분기가 달라진다.
3. **예외방향·예외반경 의미** — `ED15/ER15`, `ED25/ER25R`. 레퍼런스 화면은
   "250 km[남서 약 120 km]"로 표기한다. 남서 방향 **한 방위만** 120 km인지, 남서를 포함한
   **사분면 전체**가 120 km인지 불명. 비대칭 폴리곤 생성 알고리즘이 여기에 종속된다.
4. **`ER25R` 필드명** — 다른 필드는 `ER15` 패턴인데 이것만 끝에 `R`이 붙는다.
   명세 오타인지 실제 필드명인지 실측 필요.
5. **문서 단위 오기** — 명세에 "풍속 15km 영역"으로 적혀 있으나 문맥상 15 m/s가 맞다.
   `RAD15`/`RAD25`가 각각 강풍반경/폭풍반경에 대응하는지 실측 확인.
6. **발표 주기 시각대** — "4, 10, 16, 22시 발표"가 KST인지 UTC인지. 폴링 스케줄과
   신선도 배지 계산이 여기에 의존한다. 레퍼런스 캡처는 KST 10시 발표.
7. **진행 중 태풍 부재 시 응답** — 빈 데이터인지 오류인지. "태풍 없음"과 "조회 실패"를
   구분하지 못하면 장애를 정상으로 표시하게 된다. 🚨
8. **`typ_now.php`의 `tm` 반경** — "과거 12시간 내 발표" 기준이 발표시각인지 분석시각인지.

## 확인된 함정 🚨

- 인자 생략(`YY`, `typ`, `seq`) 시 응답이 시점에 따라 달라짐 → 운영 코드에서 전면 명시
- `FT` 무시하고 시각순 정렬 시 분석·예측 혼입 → 화면상 티가 안 남
- 서로 다른 `seq`의 예측을 합치면 경로 꼬임
- 결측 반경을 0으로 채우면 반경 0 원이 렌더됨
- 날짜변경선 통과 시 경로가 지도를 가로지름

## 다음 액션

1. Worker 골격 + `KMA_AUTH_KEY` 시크릿 설정
2. 크로반(2026 / 24호) 대상으로 세 엔드포인트 원문 수집, `fixtures/raw/`에 커밋
3. 위 미해결 질문 1·2·3·7을 픽스처로 판정하고 이 파일 갱신
