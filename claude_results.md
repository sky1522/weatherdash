# claude_results — P1 파서·도메인 모델, P2 경로 렌더링

작성: 2026-09-06 · 브랜치 `main` (direct-main)

## 결론

**P1과 P2 완료.** 지도에 크로반의 궤적이 뜬다.
분석 21개 지점은 실선과 채운 심볼로, 최신 발표(seq 21)의 예측 3개 지점은 점선과
외곽선만 있는 심볼에 리드타임 라벨을 붙여 그린다. 점을 누르면 그 시각의 발표값이 나온다.

P2 DoD 중 "기상청 화면과 경로 좌표 육안 대조"는 하지 못했다. 브라우저를 볼 수 없어서다.
그 항목만 확인이 필요하다.

## 파서가 잡아낸 것 — 그룹핑 가정이 틀렸다

P0에서 세운 규약 하나가 실제 응답과 어긋났다. 테스트를 먼저 쓴 덕에 코드가 아니라
테스트에서 걸렸다.

`SKILL.md`와 `ROADMAP`은 `(YY, TYP, SEQ)` 단위 그룹핑을 지시한다. 그런데
**`SEQ`는 분석행마다 다르다.** 발표 한 번이 분석점 하나를 남기므로, `mode=1` 응답의
분석 21행은 `SEQ` 1부터 21까지를 하나씩 가진다.

```
0,2026,24,1, 0,202609010000,...   ← 분석. seq=1
0,2026,24,21,0,202609060000,...   ← 분석. seq=21
1,2026,24,21,12,202609060000,...  ← 예측. seq=21
```

그대로 `(YY, TYP, SEQ)`로 묶으면 궤적이 **점 하나짜리 그룹 21개**로 쪼개진다.
선이 그어지지 않는다. 화면에는 점만 찍히고 오류는 나지 않는다.

두 번째로, **`typ_now`는 예측행의 `SEQ`를 `0`으로 준다.** `typ_data`는 실제 발표번호
21을 준다. 같은 데이터인데 엔드포인트마다 다르다. `SEQ` 단독으로 발표를 구분하면
`typ_now`에서 서로 다른 발표의 예측이 한 묶음에 섞인다.

고친 모델은 이렇다.

- 분석은 `(YY, TYP)` 단위로 모아 분석시각순으로 잇는다
- 예측만 발표 단위로 나눈다. 키는 `SEQ` 단독이 아니라 `(SEQ, TYP_TM)`이다

`SKILL.md` 파싱 규칙 4번과 `ROADMAP` P1 항목을 실제 구조에 맞게 고쳤다.

## 만든 것

### `lib/` — 프론트와 Worker가 함께 쓴다

| 파일 | 내용 |
|---|---|
| `types.ts` | 도메인 타입. `TrackPoint`, `ForecastSet`, `TyphoonSeries`, `Result<T>` |
| `time.ts` | UTC→KST 변환 단일 지점. 이 파일 밖에서 9시간을 더하지 않는다 |
| `compass.ts` | 16방위 변환표. `DIR`은 숫자가 아니다 |
| `parse.ts` | `disp=1, help=2` 응답 파서 |
| `geo.ts` | 날짜변경선 경도 unwrap |
| `strength.ts` | 최대풍속 → 강도. 심볼 크기로 이어진다 |

파서에서 지킨 것.

- **필드는 위치로 참조한다.** 구분자 응답에 필드명이 없고, `help=1` 안에서도
  `ER25R`과 `ER25`로 표기가 엇갈린다. 이름 기준 매핑은 그 불일치에 걸린다.
- **결측은 `null`이다.** `-999`와 `-`를 둘 다 잡는다. 0으로 채우면 반경 0 원이 그려진다.
- **`FT`로 분석과 예측을 분리한다.** 별도 배열로 유지한다. 한 배열에 담고 플래그로
  구분하는 방식은 정렬 실수가 난다.
- **`disp=0` 파서는 만들지 않았다.** 컬럼 폭이 명세에 없다.

`time.ts`에는 P0에서 확정한 발표시각 근사를 넣었다. 응답에 발표시각 필드가 없으므로
분석시각에 1시간을 더해 추정한다. 신선도 표시용이고, 근사임을 패널에 밝힌다.

### Worker — 정규화 JSON

이제 EUC-KR을 디코딩해 정규화된 JSON만 내보낸다. 프론트는 응답 텍스트 형식을 모른다.

`disp=1`과 `help=2`를 서버에서 **강제**한다. 클라이언트가 응답 형식을 바꾸지 못한다.
파서가 그 형식만 다루기 때문이다.

상태 구분을 실제로 확인했다.

```
GET /api/typhoon/data?YY=2026&typ=24&seq=21&mode=1  → {"status":"ok","data":[...]}
GET /api/typhoon/data?YY=2026&typ=99&seq=1&mode=1   → {"status":"none"}       HTTP 200
GET /api/typhoon/now?tm=190001010000&mode=1         → {"status":"none"}       HTTP 200
GET /api/typhoon/bogus                              → {"status":"error",...}  HTTP 404
```

파라미터 주입도 막힌다. `?disp=0&help=1&authKey=INJECT`를 붙여도 정규화 JSON이 나온다.
화이트리스트가 걷어내고 서버 값으로 덮어쓴다.

### 조회 흐름 — 3단

1. 목록으로 진행 중 태풍을 확정한다 (`NOW=1`)
2. `typ_now`로 최신 발표번호를 알아낸다
3. `YY`, `typ`, `seq`를 전부 명시해 `typ_data`를 다시 부른다

3단계를 생략하고 2단계 결과를 그대로 쓰지 않는다. 인자를 생략한 조회는 시점에 따라
응답이 달라져 재현이 안 되고, `typ_now`는 예측행 `SEQ`를 0으로 준다.

### 렌더링

| 소스 | 내용 |
|---|---|
| `typhoon-track-past` | 분석 경로 LineString |
| `typhoon-track-forecast` | 예측 경로 LineString. 마지막 분석점에서 분기 |
| `typhoon-points` | 분석 발표점 |
| `typhoon-forecast-points` | 예측점 |
| `typhoon-current` | 현재 위치 |

분석과 예측을 **세 가지 수단**으로 구분한다. 선의 실선과 점선, 점의 채움과 외곽선,
그리고 예측점에만 붙는 `+12h` 리드타임 라벨이다. 하나로는 예측을 관측으로 읽는다.

강도는 색이 아니라 심볼 크기로 구분한다. 색만으로 나누면 색각 이상 사용자가 읽지 못하고
흑백 인쇄에서도 사라진다. 최대풍속이 결측이면 열대저압부로 추측하지 않고 기본 크기를 쓴다.

경도 unwrap은 분석과 예측을 **이어 붙인 뒤 한 번에** 한다. 따로 처리하면 이음매에서
날짜변경선 보정이 어긋난다. 크로반은 날짜변경선을 넘지 않지만 코드 경로는 테스트로 잠갔다.

갱신은 `setData`만 쓴다. 레이어는 초기 1회만 만든다. 시각이나 발표번호가 바뀔 때마다
레이어를 다시 만들면 깜빡임과 메모리 누수가 생긴다.

### 정보 패널

지도 위 정보는 반드시 텍스트로도 존재해야 한다. 우측 패널에 레퍼런스 화면과 같은 순서로
중심위치, 중심기압, 최대풍속, 강풍반경, 폭풍반경, 강도, 진행방향, 이동속도를 둔다.

크로반은 폭풍반경이 전 구간 결측이라 패널에 하이픈으로 나온다. 숨기거나 0으로 채우지
않는다. 강풍반경은 `250 km [남서 약 120 km]` 형태로 예외방향을 함께 적는다.

시각은 KST로 적되 원본이 UTC임을 함께 남긴다.

## 검증

픽스처 기반 테스트 31건을 새로 붙였다. 전체 62건 통과, 1건 skip.

레퍼런스 캡처와의 대조를 테스트로 박아 두었다. 마지막 분석행의 위치, 기압, 최대풍속,
강풍반경, 예외방향, 예외반경이 화면 값과 일치하는지 매번 확인한다.

경계 케이스도 함께 잠갔다.

- 36시간 예측에서 강풍반경까지 결측되는 경우
- 확률반경이 분석행에서 0이고 예측행에서만 값을 가지는 경우
- 진행 중 태풍의 소멸시각 자리표시
- `help=0/1/2` 응답이 같은 결과로 파싱되는지
- `typ_now`와 `typ_data`의 예측 내용이 `SEQ`만 다르고 같은지
- 날짜변경선 unwrap
- KST 변환이 날짜를 넘길 때

```
lint       ✅
typecheck  ✅
test:unit  ✅  62 passed / 1 skipped
build      ✅  VITE_BASE=/weatherdash/ 포함
```

## 남은 🔶

**예외섹터 의미** — `ED15=SW, ER15=120`이 그 방위 한 점만인지 사분면 전체인지 불명.
P3의 비대칭 폴리곤이 여기에 종속된다. 해석을 파라미터로 두고 기본값 (A)로 간다.

**`typ_now`의 `tm` 기준** — 발표시각 기준인지 분석시각 기준인지 불명. P4 폴링 설계 전까지.

## 짚어둘 것

**기상청 화면과의 육안 대조를 못 했다.** P2 DoD의 마지막 항목이다.
값이 맞다는 것은 테스트로 확인했지만, 그 값이 화면의 올바른 위치에 찍히는지는 별개다.

**폭풍반경 레이어는 P3에서 만들지 않게 된다.** 크로반은 `RAD25`가 전 구간 결측이라,
"결측이면 레이어 자체를 생성하지 않는다"는 규칙이 바로 실행된다. 반경 0 폴리곤을
만들면 지도에 점이 찍히고 그게 폭풍반경이 있는 것처럼 보인다.

**경로는 직선으로 잇는다.** 발표 간격이 6시간이라 각진 절선이 된다. 곡선 보간은
표시용으로만 쓸 수 있고 보간점을 팝업 값으로 쓰면 안 되므로, 지금은 넣지 않았다.

**최신 발표만 그린다.** 파서는 `mode=3`의 다중 발표를 지원하지만 화면은 최신 하나만
쓴다. 발표 간 비교는 P4다.

## 다음 (P3)

1. `RAD` 원 생성. 분석행은 `RAD=0`이라 점으로 분기
2. 연속한 원들의 공통 외접선으로 콘 생성. 단순 union은 모양이 다르다
3. `RAD15`/`RAD25` 비대칭 폴리곤. 예외섹터 해석은 파라미터
4. `RAD25`가 `null`이면 레이어 미생성
5. 레이어 토글 UI
