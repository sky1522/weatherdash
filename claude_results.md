# claude_results — P0 스캐폴딩 및 응답 실측

작성: 2026-09-06 · 브랜치 `main` (direct-main)

## 결론

**P0 완료.** ROADMAP의 DoD를 충족했다. 픽스처 20건이 커밋됐고,
판정 대상 6개 항목이 전부 실제 응답 바이트를 근거로 해소됐다.

| # | 항목 | 판정 |
|---|---|---|
| 1 | 응답 인코딩 | ✅ **EUC-KR** |
| 2 | 결측 표기 | ✅ 수치 `-999`, 방위 `-`. 빈칸·`-9` 없음 |
| 3 | 태풍 부재 시 응답 | ✅ HTTP 200 + 데이터행 0개. 오류 아님 |
| 4 | `ER25R` 필드명 | ✅ 오타 아님. 단 응답 내 컬럼 헤더는 `ER25` |
| 5 | `RAD15`/`RAD25` 대응 | ✅ 강풍(15 m/s) / 폭풍(25 m/s) 반경 |
| 6 | 발표 시각 4·10·16·22시 | ✅ **KST** |

P1 이후에는 손대지 않았다. 태풍 레이어, 파서, 기하 계산 모두 만들지 않았다.

## 판정과 근거

수집 대상은 2026년 제24호 태풍 크로반(KROVANH), `YY=2026 typ=24 seq=21 mode=1`.
원문은 `fixtures/raw/`, 상세 근거는 `PROGRESS.md`의 「해소된 질문」 절에 남겼다.

### 1. 인코딩 — EUC-KR

응답 헤더가 `Content-Type: text/plain;charset=EUC-KR` 이고 실제 바이트도 일치한다.
헤더 선언과 본문이 어긋나는 경우를 대비해 바이트로 직접 확인했다.

| 픽스처 | UTF-8 디코딩 | EUC-KR 디코딩 |
|---|---|---|
| `typ_lst_disp1_help2.txt` | 37바이트에서 실패 | 성공 |
| `typ_data_disp1_help2.txt` | 83바이트에서 실패 | 성공 |
| `typ_now_disp1_help2.txt` | 83바이트에서 실패 | 성공 |

```
b'...,SW,140,\xc0\xcf\xba\xbb \xbf\xc0\xc5\xb0\xb3\xaa\xbf\xcd ...'
   EUC-KR → "일본 오키나와"
```

인증 실패 응답만 예외다. 그건 게이트웨이가 만드는 JSON이고 UTF-8이다.
두 경로의 인코딩이 다르므로 Worker에서 함께 처리하면 안 된다.

```
HTTP 401  {"result":{"status":401,"message":"유효한 인증키가 아닙니다."}}
```

### 2. 결측 — 수치는 `-999`, 방위는 `-`

`typ_data`와 `typ_now`의 데이터행 48개를 전수 조사했다.
빈 문자열도 `-9`도 나타나지 않는다.

| 필드 | 관측된 고유값 |
|---|---|
| `RAD25` 폭풍반경 | `-999` (48/48 전부 결측) |
| `ER25R` 폭풍 예외반경 | `-999` (48/48) |
| `ED25` 폭풍 예외방향 | `-` (48/48) |
| `RAD15` 강풍반경 | `240`, `250`, `260`, `-999` |
| `ER15` 강풍 예외반경 | `110`~`160`, `-999` |
| `ED15` 강풍 예외방향 | `SW`, `NNW`, `NW`, `-` |

명세에서 예상하지 못한 사실이 하나 나왔다. **강풍반경도 결측된다.**
`+36h` 예측에서 최대풍속이 15 m/s로 약해지자 세 필드가 동시에 결측됐다.

```
1,2026,24,21,36,202609060000,202609071200,28.5,132.8,NNE,16,996,15,-999,-999,110,-,-999,...
                                                              WS↑  RAD15↑ RAD25↑    ED15↑ ER15↑
```

폭풍반경만 결측된다고 가정한 코드는 여기서 조용히 깨진다.
`-999`를 그대로 숫자로 파싱하면 반경 −999 km 원을 그리게 된다.

### 3. 부재 응답 — HTTP 200, 형태는 엔드포인트마다 다름

오류가 아니다. 상태코드만으로는 "없음"과 "실패"를 구분할 수 없다.

| 조회 | HTTP | 본문 |
|---|---|---|
| `typ_data` `typ=99` | 200 | 0바이트, 완전히 빈 응답 |
| `typ_now` `tm=190001010000` | 200 | `#7777END\n` (9바이트) |

부재 형태가 엔드포인트마다 다르다. 특히 `typ_data`의 0바이트 응답은
응답 잘림과 구분되지 않는다. 판정 규칙을 아래로 고정했다.

- HTTP 200 + 데이터행 0개 → `status: 'none'`
- 그 외 전부 → `status: 'error'`

### 4. `ER25R` — 오타가 아니다. 응답 내부가 불일치한다

`help=1` 응답 하나가 두 표기를 함께 쓴다. 항목 설명은 `ER25R`, 컬럼 헤더는 `ER25`다.

```
# 21. ER25R  : 풍속 25km 영역에서 예외반경 (km)
# FT   YY  TYP  SEQ ... ED15  ER15 LOC----------------ED25 ER25
```

`disp=1` 구분자 응답에는 필드명이 실려 오지 않으므로 파싱에는 영향이 없다.
대신 파서가 필드명이 아니라 **위치**로 참조해야 한다는 뜻이다.
21번째 필드가 폭풍 예외반경이다. 이름 기준 매핑은 이 불일치에 걸린다.

### 5. `RAD15`/`RAD25` — 강풍/폭풍반경이 맞다

`help=1` 항목 설명은 `RAD` 계열을 정확히 적고 있다.

```
# 14. RAD15  : 15m/s 반경 (km)
# 15. RAD25  : 25m/s 반경 (km)
# 16. RAD    : 70%이상 예상확률반경 (km)
```

"15km" 오기는 `RAD` 계열이 아니라 예외방향·예외반경 설명 쪽에 있었다.
`# 17. ED15 : 풍속 15km 영역에서 예외방향`. 문맥상 15 m/s가 맞다.

레퍼런스 캡처와 대조했다. `seq=21` 분석행이 화면 값과 전부 일치한다.

```
0,2026,24,21,0,202609060000,202609060000,25.9,129.3,NE,14,992,21,250,-999,0,SW,120,...,-,-999,=
```

| 화면 표기 | 필드 | 값 |
|---|---|---|
| 25.9°N 129.3°E | `LAT`/`LON` | 25.9 / 129.3 |
| 992 hPa | `PS` | 992 |
| 최대풍속 21 m/s | `WS` | 21 |
| 강풍반경 250 km[남서 약 120 km] | `RAD15`/`ED15`/`ER15` | 250 / SW / 120 |
| 폭풍반경 `-` | `RAD25` | `-999` |
| 북동진 14 km/h | `DIR`/`SP` | NE / 14 |

부수적으로 확인된 것이 하나 있다. `RAD`(70% 확률반경)는 분석행에서 전부 `0`이고
예측행에서만 `40`/`80`/`110`이다. SKILL.md 2부가 예고한 "현재 위치 원 반경 0" 분기가
실제로 필요하다.

### 6. 발표 시각 — KST

분석시각(`TYP_TM`)은 UTC `00/06/12/18` 정시로만 나타난다. KST로는 `09/15/21/03`이다.

```
202609010000, 202609010600, 202609011200, 202609011800, 202609020000, ...
```

레퍼런스 캡처는 "2026-09-06 10:00 발표, 09시 현재"다. 그 09시 값이 분석행
`TYP_TM=202609060000`(UTC)과 정확히 일치한다. 09 KST = 00 UTC 분석 → 10 KST 발표.
같은 관계를 네 회차에 적용하면 `03/09/15/21 KST` 분석 → `04/10/16/22 KST` 발표가 된다.

이 판정은 API 응답 단독으로는 나오지 않는다. 응답에 발표시각 필드가 없기 때문이다.
분석시각(응답)과 발표시각(레퍼런스 캡처)을 이어 붙인 결과다. 근거의 한쪽 끝이
캡처라는 점을 밝혀 둔다.

## 남은 🔶 두 건

P0 범위 밖이고, 픽스처로도 해소되지 않았다. 추측으로 메우지 않았다.

**예외섹터 의미** — 응답은 `ED15=SW, ER15=120`처럼 방위 하나와 반경 하나만 준다.
그 방위 한 점만 120 km인지 남서 사분면 전체가 120 km인지 판별할 정보가 응답에 없다.
SKILL.md 2부의 해석 (A)/(B) 파라미터화를 유지한다. 기본값 (A). P3에서 다룬다.

**`typ_now`의 `tm` 기준** — "과거 12시간 내 발표"가 발표시각 기준인지 분석시각
기준인지 미확정이다. `tm=202609060300`(UTC) 조회 결과가 `typ_data seq=21`과 사실상
같았는데, 해당 발표의 분석시각(00 UTC)과 발표시각(약 01 UTC)이 12시간 창에 모두 들어가
두 해석을 구분하지 못한다. 경계에 걸치는 `tm`으로 다시 조회해야 한다. P4에서 다룬다.

## 만든 것

### 프론트

Vite 6 + TypeScript strict + MapLibre GL JS 5 + pmtiles 4.
`src/basemap-style.ts`에 Protomaps v4 스키마 기준 무채색 스타일을 직접 작성해
런타임 의존성을 `maplibre-gl`, `pmtiles` 둘로 묶었다.

초기 뷰는 북서태평양(`center [138, 22]`, `zoom 3`). `maxBounds` 동쪽 끝을 경도 200까지
열어 두어 날짜변경선을 넘는 경로가 P2에서 잘리지 않게 했다.
레이어 id `place-labels`는 P2에서 태풍 면 레이어를 `beforeId`로 끼울 기준점이다.

베이스맵 URL은 `VITE_PMTILES_URL`로 교체 가능하다. 기본값은 Worker의 타일 중계 경로
`/api/basemap`이다. 원본은 Protomaps 공개 데모 PMTiles이고, 그 버킷이 CORS 헤더를
주지 않아 직접 읽을 수 없어 중계를 거친다.

### Worker 프록시

라우트 세 개를 표로 매핑했다. 표에 없는 경로는 프록시하지 않는다.

| 라우트 | 업스트림 | 허용 파라미터 |
|---|---|---|
| `/api/typhoon/list` | `typ01/url/typ_lst.php` | `YY, disp, help, mode` |
| `/api/typhoon/data` | `typ01/url/typ_data.php` | `YY, typ, seq, mode, disp, help` |
| `/api/typhoon/now` | `typ01/url/typ_now.php` | `tm, mode, disp, help` |

파라미터를 화이트리스트로 막았다. 막지 않으면 클라이언트가 `authKey`를 실어 보내
서버 시크릿을 덮어쓸 수 있다. 응답은 정규화 없이 바이트 그대로 통과시킨다.
EUC-KR 디코딩과 JSON 정규화는 P1에서 붙인다.

`worker/secret.ts`의 `redact()`가 시크릿의 원문·URL 인코딩본·디코딩본을 모두 지우고,
값을 모르는 상황에서도 `authKey=...` 패턴 자체를 마스킹한다. 가장 위험한 경로는
`fetch` 예외다. 예외 메시지에 요청 URL이 통째로 실리고 거기에 키가 들어 있다.
그 경로를 유닛 테스트로 잠갔다. `no-console` 규칙으로 워커에서 로그 자체를 막았다.

키 미설정은 `auth_key_unset` 오류로 명시 반환한다. 빈 응답으로 삼키지 않는다.

### 픽스처

`scripts/fetch-fixtures.mjs` (`pnpm fixtures`). 20건을 `fixtures/raw/`에 저장했다.

3 엔드포인트 × `disp` 2종 × `help` 3종 = 18건, 부재 응답 판정용 2건.
전부 HTTP 200, `Content-Type: text/plain;charset=EUC-KR`.

발표번호를 먼저 확정한 뒤(`seq=21`) 이후 수집에서 `seq`를 명시했다.
인자를 생략하면 응답이 시점에 따라 달라져 픽스처로서 재현이 안 된다.
`manifest.json`에 HTTP 상태, `Content-Type`, 바이트 수, SHA-256, 요청 파라미터를 남겼다.
`authKey`는 기록하지 않는다. 저장 직전에 응답 본문에 키가 에코됐는지도 검사한다.

## 도중에 잡은 문제

### 픽스처 바이트가 커밋 시점에 깨질 뻔했다

저장소가 `core.autocrlf=true`인데 `.gitattributes`가 없었다. 이 상태로
`fixtures/raw/*.txt`를 커밋하면 git이 텍스트로 판단해 커밋 시 CRLF→LF,
체크아웃 시 LF→CRLF로 바꾼다. "원문 바이트 그대로"가 파일을 쓰는 순간이 아니라
**커밋하는 순간** 깨진다.

인코딩 판정과 결측 판정이 전부 이 바이트 위에서 이뤄지므로, 눈치채지 못한 채
잘못된 판정을 내리고 그 위에 P1 파서를 올렸을 것이다.

`.gitattributes`로 `fixtures/raw/** -text -diff`를 걸어 변환을 금지했고
`git check-attr`로 `text: unset`을 확인했다. 여기에 더해 `tests/fixtures.test.ts`가
매니페스트의 SHA-256과 바이트 수를 실제 파일과 대조한다. 20건 전부 통과한다.

### 발표번호 자동 확정이 빗나갔다

응답의 첫 필드가 `YY`가 아니라 `FT`(0=분석, 1=예측)였다. 정규식이 매칭되지 않아
1차 수집이 `seq` 없이 이뤄졌다. 형식을 확인하고 고친 뒤 재수집했다.

### GitHub Pages를 복구했다

루트 `index.html`을 Vite 엔트리로 교체하면서 기존 Pages 배포가 깨졌다.
Pages가 저장소 루트를 그대로 서빙하는 설정이었는데 새 엔트리는 저장소에 없는
`/src/main.ts`를 부른다.

`.github/workflows/ci.yml`을 추가해 빌드 산출물을 배포하도록 바꿨다.
같은 워크플로가 `lint → typecheck → test:unit → build` 게이트를 원격에서 돌린다.
CLAUDE.md 게이트의 마지막 항목인 remote CI가 이걸로 채워진다.
Pages 하위 경로용 에셋 경로는 `VITE_BASE`로 주입한다.

### 인증키

`.dev.vars`가 세 차례 생성되지 않아 막혀 있었다. 확인 결과 이 PC의 다른 프로젝트가
같은 API를 쓰고 있었고, 사용자 승인을 받아 `C:\dev\wavyon\.env.local`의 `KMA_API_KEY`를
가져왔다. 세 후보 중 실제 호출로 동작하는 것을 골랐다. 값은 출력하지 않았다.
`.dev.vars`는 `.gitignore` 대상이라 커밋되지 않는다.

## 파일 이동

- `index.html` → `legacy/console.html` (기존 수원 기상 콘솔). 루트는 Vite 엔트리로 교체
- `SKILL.md` → `.claude/skills/typhoon-map/SKILL.md` (CLAUDE.md가 가리키던 경로)
- `CLAUDE.md`의 스킬 목록을 실제 파일 구성과 맞춤. 세 개를 나열했으나 실제로는
  병합된 단일 파일이고, 파일 스스로 한 파일로 유지한다고 밝히므로 목록 쪽을 고침
- `SKILL.md`의 🔶 마커 중 해소된 4건을 ✅로 갱신. 섹터 해석 1건은 🔶로 남김

## 검증 게이트

```
lint       ✅
typecheck  ✅
test:unit  ✅  31 passed / 1 skipped
build      ✅  VITE_BASE=/weatherdash/ 검증 포함
commit     ✅  파일 개별 add. git add -A 미사용
push       ✅  origin/main
remote CI  ✅
```

`build`에서 청크 1.08 MB 경고가 난다. MapLibre 본체 크기다.
코드 스플리팅은 요청 범위 밖이라 손대지 않았다.

## 짚어둘 것

**베이스맵이 CORS로 막혀 있었다. Worker 중계로 고쳤다.**
Protomaps 데모 버킷은 `Access-Control-Allow-Origin`을 주지 않아 브라우저가 직접
읽을 수 없다. 초기 구현은 그 URL을 프론트에서 바로 열려고 해서 "베이스맵 로드 실패"가 났다.
Worker에 `/api/basemap`을 추가해 Range 요청을 중계하고 CORS 헤더를 붙인다.

로컬에서 끝까지 확인했다. PMTiles 헤더, 메타데이터, 그리고 북서태평양 초기 뷰에
해당하는 실제 벡터 타일까지 중계 경로로 받아진다.

```
GET /api/basemap  Range: bytes=0-127
  → 206, Content-Range: bytes 0-127/137781495687, Access-Control-Allow-Origin: *
  → 매직바이트 "PMTiles"
tile z3/7/3 → 16096 bytes
layers: boundaries, buildings, earth, landcover, landuse, places, pois, roads, water
```

스타일이 참조하는 소스 레이어 이름(`earth`, `landuse`, `water`, `roads`, `boundaries`,
`places`)이 실제 파일의 레이어와 일치하는 것도 메타데이터로 확인했다. 추측이었던 부분이다.

🚨 **배포된 Pages 페이지는 아직 베이스맵이 뜨지 않는다.** Pages는 정적 호스팅이라
중계할 Worker가 없다. 해소하려면 둘 중 하나가 필요하다.

- Worker를 Cloudflare에 배포하고 `VITE_PMTILES_URL`을 그 URL로 지정
- 북서태평양 영역만 추출한 PMTiles를 `public/`에 두고 같은 출처에서 서빙

운영 트래픽을 공개 데모 버킷에 얹으면 안 된다는 점은 그대로다.

**Node 20에서 최신 wrangler가 돌지 않는다.** wrangler 4.100 이상이 Node 22를 요구한다.
설치된 런타임이 20.19.6이라 Node 20 지원 마지막 계열인 `~4.86.0`으로 고정했다.
Node 22로 올리면 핀을 풀 수 있다. CI는 Node 22를 쓴다.

**`typ_lst`의 진행 중 태풍 판정 필드는 `NOW`다.** `1`이 진행 중, `2`가 종료다.
크로반은 `NOW=1`, `TM_ED=210012310000`으로 종료시각이 미정 상태다.
이 자리표시 값을 그대로 날짜로 파싱하면 2100년이 된다. P1에서 처리해야 한다.

## 다음 (P1)

1. `lib/time.ts` — UTC→KST 변환 단일 지점, 16방위 변환표
2. `parseTyphoonList()` / `parseTyphoonData()` — `disp=1, help=2` 기준.
   필드는 위치로 참조. `-999`/`-` → `null`
3. `FT` 기준 `analysis[]` / `forecast[]` 분리, `(YY, TYP, SEQ)` 그룹핑
4. Worker에 EUC-KR→UTF-8 디코딩과 정규화 JSON 추가, `ok` / `none` / `error` 구분
5. 픽스처 20건 전부에 대한 파서 스냅샷 테스트
