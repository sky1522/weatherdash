---
description: 이전 작업 내용을 파악하고 이어갈 준비를 한다. 구현은 시작하지 않는다.
argument-hint: [집중해서 볼 부분 (선택)]
---

이 저장소의 작업을 이어받는다. **파악만 하고 멈춘다. 승인 없이 구현에 들어가지 않는다.**

$ARGUMENTS

## 1. 규약과 상태를 읽는다

아래 순서로 읽는다. 뒤의 문서가 앞의 문서와 충돌하면 `CLAUDE.md`가 우선이다.

1. `CLAUDE.md` — 작업 규약, 검증 게이트, 이 도메인의 함정
2. `PROGRESS.md` — 현재 단계, 결정 기록, 해소된 질문과 미해결 질문
3. `ROADMAP.md` — 단계별 완료 기준(DoD). 어디까지 체크되어 있는지
4. `.claude/skills/typhoon-map/SKILL.md` — API·파싱·기하·렌더링 규약
5. `claude_results.md` — 직전 작업의 결과 보고

## 2. 저장소 상태를 확인한다

- 현재 브랜치와 최근 커밋 5개
- 커밋되지 않은 변경이 있는지
- `fixtures/raw/manifest.json`이 있는지, 수집 대상이 무엇인지

## 3. 실행 환경을 확인한다

- `node_modules`가 설치되어 있는지
- `.dev.vars` 파일이 **존재하는지**. 🚨 인증키 값은 절대 출력하지 않는다.
  없으면 `.dev.vars.example`을 복사해 채워야 한다고 알린다.
- 개발 서버가 떠 있는지 (`:5173` 프론트, `:8787` Worker)

## 4. 보고한다

아래 내용을 간결하게 정리해서 보고한다.

- **현재 단계** — ROADMAP 기준 어디까지 완료됐고 다음이 무엇인지
- **직전 작업이 한 일** — `claude_results.md`와 마지막 커밋 기준
- **미해결 🔶** — 아직 실측으로 확정되지 않은 항목. 🚨 이걸 추측으로 메우지 않는다
- **막힌 것** — 인증키 미설정, 게이트 실패, 미완 DoD 등 진행을 막는 것
- **제안하는 다음 작업** — 한두 개. 실행하지는 않는다

## 지킬 것

- 🔶 항목을 임의로 확정하지 않는다. 모르면 모른다고 보고한다.
- 문서에 적힌 내용이 코드와 어긋나면 그 사실을 보고한다. 조용히 한쪽에 맞추지 않는다.
- 이 명령은 읽기만 한다. 파일을 고치거나 커밋하지 않는다.
