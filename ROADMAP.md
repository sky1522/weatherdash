# ROADMAP

각 단계는 **완료 기준(DoD)** 을 만족해야 다음으로 넘어간다.
"화면에 뭔가 나온다"는 완료가 아니다. 값이 맞는지 검증된 상태가 완료다.

---

## P0 — 스캐폴딩 및 응답 실측

목표: 코드를 쓰기 전에 실제 응답이 어떻게 생겼는지 확정한다.

- [x] Vite + TypeScript + MapLibre 기본 셋업, PMTiles 베이스맵 표시
- [x] Cloudflare Worker 프록시 골격 (`/api/typhoon/*`), `KMA_AUTH_KEY` 시크릿 바인딩
- [x] 3개 엔드포인트 원문 응답을 `fixtures/raw/` 에 저장
      (`typ_lst`, `typ_data`, `typ_now` × `disp=0/1` × `help=0/1/2`, 부재 응답 2건 포함 총 20건)
- [x] 응답 인코딩 확정 ✅ **EUC-KR** — UTF-8 디코딩 실패, 헤더도 `charset=EUC-KR`
- [x] 결측값 표기 확정 ✅ 수치는 `-999`, 방위는 `-`. 빈칸·`-9`는 없음
- [x] 진행 중 태풍이 없을 때의 응답 형태 확정 ✅ HTTP 200 + 데이터행 0개 (오류 아님)
- [x] `ER25R` 필드명 ✅ 오타 아님. 단 응답 내 컬럼 헤더는 `ER25`로 불일치
- [x] `RAD15`/`RAD25` 대응 ✅ 각각 강풍(15 m/s)/폭풍(25 m/s) 반경. 레퍼런스와 일치
- [x] 발표 시각대 ✅ 4·10·16·22시는 **KST**. 분석시각은 UTC 00/06/12/18

**DoD 충족.** 근거는 `PROGRESS.md` 「해소된 질문」 절, 원문은 `fixtures/raw/`.

---

## P1 — 파서와 도메인 모델

목표: 텍스트 응답 → 타입 안전한 도메인 객체.

- [x] `parseTyphoonList()` / `parseTyphoonData()` 구현 (`disp=1, help=2` 기준)
- [x] `disp=0` 고정폭 응답 파서는 만들지 않는다 — 컬럼 폭이 명세에 없음 🚨
- [x] `FT` 기준으로 `analysis[]` / `forecast[]` 분리
- [x] 그룹핑 ✅ **`SEQ`는 분석행마다 다르다.** 분석은 `(YY, TYP)` 단위로 모으고,
      예측만 `(SEQ, TYP_TM)` 단위로 나눈다. `typ_now`는 예측행 `SEQ`를 0으로 준다 🚨
- [x] UTC→KST 변환 단일 지점 `lib/time.ts`
- [x] `DIR` 16방위 → 각도 변환표
- [x] 결측 → `null` 정규화, 0으로 채우지 않음 🚨
- [x] 픽스처 기반 유닛 테스트 (필드 수, 타입, 결측, 경계 케이스)
- [x] Worker가 EUC-KR을 디코딩해 정규화 JSON을 반환. `ok` / `none` / `error` 구분 🚨

**DoD 충족.** 픽스처 기반 테스트 31건 green.

---

## P2 — 경로 렌더링

목표: 레퍼런스 화면의 검은 궤적을 재현한다.

- [x] 분석 경로: 실선 + 시각별 태풍 심볼, 강도에 따라 심볼 변화
- [x] 예측 경로: 점선, 현재 위치를 기점으로 분기
- [x] 현재 위치 강조 마커
- [x] 경도 unwrap (날짜변경선) 🚨
- [x] 클릭 시 해당 시각 상세 팝업 — 호버 커서 포함
- [x] 텍스트 정보 패널 (지도만으로 정보를 전달하지 않는다)

**DoD**: 동일 태풍·동일 seq에서 기상청 화면과 경로 좌표가 육안 대조로 일치. — 육안 대조 대기.

---

## P3 — 확률반경과 풍속반경

목표: 노란색 예보원과 강풍/폭풍반경.

- [ ] `RAD`(70% 확률반경) 원 생성 — 예보 시각별
- [ ] 연속한 원들의 공통 외접선으로 콘(포락선) 생성
- [ ] `RAD15` / `RAD25` 원 + `ED15/ER15`, `ED25/ER25R` 예외 섹터 반영한 비대칭 폴리곤 🔶
      예외방향이 "그 방향만 반경이 다름"인지 "그 방향부터 시계방향 사분면"인지 명세 불명 🚨
- [ ] 폭풍반경 결측 시 레이어 자체를 숨김 (반경 0 원 금지) 🚨
- [ ] 레이어 토글 UI (확률반경 / 강풍반경 / 폭풍반경 / 예측경로)

**DoD**: 레퍼런스 화면과 콘 폭·반경이 대조 가능한 수준으로 일치. 예외섹터 해석은 근거와 함께 기록.

---

## P4 — 시간축과 정보 패널

- [ ] 발표 시각 타임라인 (4·10·16·22시 정시 발표 + 수시)
- [ ] 예보 시각 스크러버, 재생 애니메이션
- [ ] 우측 정보 패널: 중심위치/중심기압/최대풍속/강도/진행방향/이동속도
- [ ] seq 비교 모드 — 이전 발표 대비 경로 이동량 표시
- [ ] 태풍 선택 드롭다운 (해당 연도 목록)

**DoD**: 임의 시각을 선택하면 패널 값과 지도 상태가 동일 시각을 가리킨다.

---

## P5 — 운영 견고화

- [ ] Worker 캐시 (발표 주기 기반 TTL), 레이트리밋
- [ ] API 실패/빈 응답/부분 응답 상태를 화면에서 구분 표시 🚨
- [ ] 데이터 신선도 배지 (마지막 발표로부터 경과 시간)
- [ ] Playwright E2E: 태풍 선택 → 경로 표시 → 레이어 토글 → 시각 이동
- [ ] 접근성: 키보드 포커스, 축소 모션 존중, 색 외 구분 수단

**DoD**: 전체 검증 게이트 통과 + 원격 CI green.

---

## 이번 범위 밖

- 종료된 태풍 재생 (베스트트랙 / `typ02` 연동은 별도 단계)
- 앙상블·다중기관(JTWC/JMA) 경로 비교
- 위성·레이더 오버레이
- 영향예보 / 자체 위험도 산출
