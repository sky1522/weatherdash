# CLAUDE.md

이 저장소에서 Claude Code가 지켜야 할 규약.

## 프로젝트 한 줄 정의

기상청 API 허브 태풍 데이터를 MapLibre 지도에 렌더링하는 뷰어. 분석/예측 경로, 70% 확률반경,
강풍·폭풍반경을 시간축과 함께 표시한다.

## 작업 방식

- 구현은 Claude Code가 전담한다. 조사·검증 목적에 한해 Codex를 쓸 수 있다.
- direct-main fast mode. 별도 지시가 없으면 브랜치/PR을 만들지 않는다.
- `git add -A` 금지. 변경 파일을 개별로 add 한다.
- 결과 보고는 `claude_results.md`에 **덮어쓰기**로 남긴다.
- 호칭은 "성관님" 또는 생략.

## 검증 게이트

커밋 전 아래 순서를 통과해야 한다. 하나라도 실패하면 커밋하지 않는다.

```
lint → typecheck → test:unit → build → targeted E2E → commit → push → remote CI
```

파서 관련 변경은 `test:unit`에 **실제 응답 고정 픽스처(fixture)** 테스트가 반드시 포함되어야 한다.
API 응답 텍스트를 스냅샷으로 박아두고 회귀를 잡는다.

## 이 도메인에서 특히 조심할 것

기상 데이터는 **틀려도 화면이 정상으로 보인다.** 예외를 던지지 않고 조용히 잘못된 값을 그린다.
아래는 실수하면 눈치채기 어려운 항목이다.

1. **시각은 전부 UTC다.** `TYP_TM`, `FT_TM`, `tm` 파라미터 모두 UTC. 화면 표기는 KST.
   변환 지점을 한 곳(`lib/time.ts`)으로 모으고, 그 밖에서 `+9`를 직접 더하지 않는다.
2. **분석과 예측을 섞지 않는다.** `FT` 필드가 0이면 분석, 1이면 예측이다. 이걸 무시하고
   시각순으로만 정렬하면 과거 궤적에 예측점이 끼어든다. 화면상 구분이 안 되므로 조용히 틀린다.
3. **발표번호(seq)가 다르면 다른 예측이다.** 여러 seq의 예측을 한 배열에 합치면 경로가 꼬인다.
   항상 `(YY, TYP, SEQ)` 단위로 그룹핑한다.
4. **`DIR`은 숫자가 아니라 16방위 기호다.** (`NE`, `NNE` …) 각도 변환표를 통해서만 쓴다.
5. **결측값 처리.** 폭풍반경은 약한 태풍에서 비어 있다(레퍼런스 화면의 `-`). 0으로 채우면
   반경 0의 원을 그리게 된다. 결측은 `null`로 유지하고 렌더 단계에서 스킵한다.
6. **인자 생략 = 비결정적 응답.** `YY` 생략 시 올해, `typ` 생략 시 마지막 태풍, `seq` 생략 시
   마지막 발표. 운영 코드에서는 전부 명시한다. 재현 불가능한 버그의 주 원인.
7. **경도 180도 넘김.** 북서태평양 태풍은 날짜변경선을 넘을 수 있다. 경로 좌표는 연속성을
   유지하도록 unwrap 한다.

## 주석 체계

문서와 코드 주석에서 아래 마커를 사용한다.

- 🚨 — 조용히 데이터가 깨지는 함정. 반드시 방어 코드와 테스트가 붙어야 함
- 🔶 — 명세가 모호해 실측 검증이 필요한 항목. 검증 전에는 가정을 코드에 박지 말 것
- ✅ — 실제 응답으로 검증 완료

🔶 항목을 임의로 확정하지 않는다. 모르면 `PROGRESS.md`의 미해결 질문에 추가하고 보고한다.

## 슬래시 명령

세션 시작과 끝에 쓴다. 정의는 `.claude/commands/` 에 있다.

| 명령 | 용도 |
|---|---|
| `/resume` | 이전 작업 파악. 규약·상태·환경을 읽고 다음 할 일을 보고한다. 구현은 시작하지 않는다 |
| `/wrap` | 작업 마무리. 검증 게이트 → 문서 갱신 → 개별 add 커밋 → 푸시 → 원격 CI 확인 |

## 스킬

`.claude/skills/` 아래 규약을 작업 전에 읽는다.

- `typhoon-map` (`.claude/skills/typhoon-map/SKILL.md`) — 아래 세 영역을 한 파일로 유지한다.
  세 영역이 서로의 가정에 의존하므로 분리하지 않는다.
  - 1부 API와 파싱 — 요청 파라미터, 응답 텍스트 파싱, 필드 매핑
  - 2부 기하 계산 — 확률반경 콘, 비대칭 풍속반경 폴리곤 생성
  - 3부 렌더링 — 레이어 구성, 소스 갱신, 스타일 토큰

## 하지 말 것

- `authKey`를 프론트 코드·번들·커밋에 넣기
- 실패한 API 호출을 빈 배열로 삼켜서 "태풍 없음"으로 표시하기 (오류 상태를 명시적으로 구분)
- 검증 안 된 필드 의미를 추측으로 채워 넣기
- 요청받지 않은 리팩터링·의존성 추가
